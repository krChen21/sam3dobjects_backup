
python -m pip install modelscope
modelscope download --model facebook/sam-3d-objects --local_dir /home/ubuntu/project/asset3Drecons/sam-3d-objects/checkpoints/
git config --global url."git@github.com:".insteadOf "https://github.com/"
git config --global --unset-all url.git@github.com:.insteadOf

export HF_ENDPOINT=https://hf-mirror.com


cd /home/ubuntu/project/asset3Drecons/sam-3d-objects && conda activate sam3d-objects
python demo.py

# 全格式导出（Gaussian splat / mesh / GLB）
cd /home/ubuntu/project/asset3Drecons/sam-3d-objects && conda activate sam3d-objects
python demo_export_all.py \
  --image notebook/images/demo/jia1.jpg \
  --mask notebook/images/demo/1.png \
  --out outputs/tai_export_all \
  --seed 42 \
  --mesh-postprocess \
  --texture-baking \
  --print-output-keys

cd /home/ubuntu/project/asset3Drecons/sam-3d-objects && conda activate sam3d-objects
python load_splat.py splat.ply --interactive



```bash
git config --global url."git@github.com:".insteadOf "https://github.com/"

export HF_ENDPOINT=https://hf-mirror.com

cd /home/ubuntu/sam-3d-objects && conda activate sam3d-objects
python demo.py

python load_splat.py splat.ply --interactive

python scripts/run_sam3d_export_mesh_glb.py \
  --image notebook/images/tai_demo/tai4.jpg \
  --mask notebook/images/tai_demo/1.png \
  --config checkpoints/hf/pipeline.yaml \
  --out /home/ubuntu/sam-3d-objects/outputs/tai_mesh_export \
  --seed 42

python demo_export_all.py \
  --image notebook/images/tai_demo/tai4.jpg \
  --mask notebook/images/tai_demo/1.png \
  --out outputs/tai_export_all \
  --seed 42 \
  --mesh-postprocess \
  --texture-baking \
  --print-output-keys
```


## demo_export_all.py 全格式导出

脚本路径：`demo_export_all.py`（与 `demo.py` 同目录）

作用：在 `demo.py` 相同推理路径下，一次性导出 Gaussian splat、三角 mesh、GLB 等所有可用格式。相比 `demo.py` 只保存 `splat.ply`，本脚本会通过 `inference._pipeline.run` 开启 mesh 后处理与纹理烘焙。

### 推荐命令

```bash
cd /home/ubuntu/sam-3d-objects && conda activate sam3d-objects

python demo_export_all.py \
  --image notebook/images/tai_demo/tai4.jpg \
  --mask notebook/images/tai_demo/1.png \
  --out outputs/tai_export_all \
  --seed 42 \
  --mesh-postprocess \
  --texture-baking \
  --print-output-keys
```

### 常用参数

| 参数 | 说明 |
|------|------|
| `--image` | 输入 RGB 图片 |
| `--mask` | 输入 mask 图片（与 demo 一致，`mask > 0` 为前景） |
| `--out` | 输出目录 |
| `--seed` | 随机种子 |
| `--config` | pipeline 配置，默认 `checkpoints/hf/pipeline.yaml` |
| `--mesh-postprocess` | 开启 mesh 简化、填洞等后处理 |
| `--texture-baking` | 开启多视角纹理烘焙（GLB 带贴图，耗时更长） |
| `--print-output-keys` | 打印推理结果 `output` 的 keys 和 mesh/glb 详情 |
| `--dry-run` | 只推理并打印摘要，不写大文件 |

### 可选导出开关（默认全部导出）

不传任何 `--export-*` 时，默认尝试导出全部格式。若只想导出部分，可显式指定：

```bash
python demo_export_all.py \
  --image notebook/images/tai_demo/tai4.jpg \
  --mask notebook/images/tai_demo/1.png \
  --out outputs/tai_export_all \
  --export-gs --export-glb --export-obj
```

| 参数 | 输出文件 |
|------|----------|
| `--export-gs` | `splat.ply`、`gaussian.ply`（3D Gaussian splat，无 face） |
| `--export-glb` | `object.glb` |
| `--export-obj` | `mesh.obj` |
| `--export-stl` | `mesh.stl` |
| `--export-ply` | `mesh.ply`（三角 mesh，含 face，不是 splat） |
| `--export-mesh` | 等同于导出 obj/stl/ply |

### 输出目录结构

```
outputs/tai_export_all/
├── splat.ply          # 3D Gaussian splat（用于 load_splat.py 可视化）
├── gaussian.ply       # 同上，来自 output["gaussian"]
├── object.glb         # 后处理 mesh + 纹理（推荐用于查看/转 MuJoCo）
├── mesh.obj
├── mesh.stl
├── mesh.ply           # 三角 mesh
├── metadata.json      # 导出记录、顶点面数、warnings
└── logs/
    └── output_summary.txt
```

### 与 demo.py 的区别

| | demo.py | demo_export_all.py |
|---|---------|-------------------|
| 推理入口 | `Inference.__call__` | 复用同一 `Inference`，调用 `_pipeline.run` |
| mesh 后处理 | 关闭 | `--mesh-postprocess` 开启 |
| 纹理烘焙 | 关闭 | `--texture-baking` 开启 |
| 默认输出 | 仅 `splat.ply` | splat + mesh + glb + metadata |

### 注意事项

- 纹理烘焙依赖 `gsplat`、`xatlas` 等；若失败会打印 `texture baking dependency missing` 并回退导出无纹理 mesh/glb。
- `splat.ply` 是 Gaussian 点云，**不能**直接当 mesh 做 Poisson 重建；转 MuJoCo 请优先用 `object.glb` 或 `mesh.obj`。
- 仅查看 splat 可用：`python load_splat.py outputs/tai_export_all/splat.ply --interactive`